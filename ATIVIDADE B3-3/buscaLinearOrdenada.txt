int busca_linear_ordenada(int arr[], int n, int alvo) {
    for (int i = 0; i < n; i++) {
        if (arr[i] > alvo) {
            return -1;
        }
        if (arr[i] == alvo) {
            return i;
        }
    }
    return -1;
}
